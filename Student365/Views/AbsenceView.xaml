<UserControl x:Class="Student365.Views.AbsenceWiew"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Student365.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">

        <StackPanel Orientation="Vertical">
            <StackPanel HorizontalAlignment="Left"   Orientation="Horizontal" VerticalAlignment="Center">
                <ComboBox Foreground="Black"
                          Style="{StaticResource MaterialDesignOutlinedComboBox}"
                    materialDesign:HintAssist.Hint="Some item"
                    ItemsSource="{Binding SubjectList}"
                    Width="200"
                          SelectedItem="{Binding Selected}" />
                <TextBlock Text="Choose Date"  FontSize="25" VerticalAlignment="Center" Foreground="Black"></TextBlock>
                <DatePicker Margin="20,0,0,0" Foreground="Black" Text="{Binding Date,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"></DatePicker>
                <TextBlock Text="Write a reason" VerticalAlignment="Center" FontSize="25" Padding="10,0,0,0" Foreground="Black"></TextBlock>
                <TextBox Text="{Binding Reason,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Foreground="Black"  Padding="0,0,10,0" Width="120" Cursor="Arrow"></TextBox>
                <Button Style="{StaticResource MaterialDesignIconButton}" Foreground="Black" Background="Green" Command="{Binding AddAbsenceCommand}" Cursor="Hand">
                    <materialDesign:PackIcon Kind="Plus"></materialDesign:PackIcon>
                </Button>
            </StackPanel>
            <DataGrid ItemsSource="{Binding Absences,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">
            </DataGrid>
            <DataGrid ItemsSource="{Binding AbsenceCount,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>