<UserControl x:Class="Student365.Views.GroupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Student365.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid Style="{StaticResource MaterialDesignDataGrid}" ItemsSource="{Binding Students}" AutoGenerateColumns="False" IsReadOnly="True" HorizontalAlignment="Center"  VerticalAlignment="Stretch" FontSize="20" Padding="2">
            <DataGrid.Columns>
                <DataGridTextColumn CellStyle="{StaticResource MaterialDesignDataGridCell}" Header="Name" Binding="{Binding Name}"></DataGridTextColumn>
                <DataGridTextColumn CellStyle="{StaticResource MaterialDesignDataGridCell}" Header="Group" Binding="{Binding Group}"></DataGridTextColumn>
                <DataGridTextColumn CellStyle="{StaticResource MaterialDesignDataGridCell}" Header="Kurs" Binding="{Binding Kurs}"></DataGridTextColumn>
                <DataGridTextColumn CellStyle="{StaticResource MaterialDesignDataGridCell}" Header="Sub Group" Binding="{Binding SubGroup}"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>